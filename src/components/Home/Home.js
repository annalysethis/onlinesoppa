import React, { Component } from "react";
import Form from "./../Form";

export class Home extends Component {
  render() {
    return (
      <div>
        <Form />
      </div>
    );
  }
}

export default Home;
