import React from 'react'
import { data } from "../Data/data";
import SoupItem from "./../SoupItem"

export const SoupList = () => {
    return (
        <div>
        {/* {data.map(item =>(
                <SoupItem title={item.title} price={item.price} key={item.id}/>
            ))} */}
        </div>
    )
}

export default SoupList;
